import 'package:flutter/material.dart';
import 'package:new_grad_proj/my_profile_menu.dart';

class person extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(body: MyProfile());
}
