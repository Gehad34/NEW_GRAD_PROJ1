import 'package:flutter/material.dart';
import 'package:new_grad_proj/screens/creating_box.dart';

class add_page extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(body: CreateBox());
}
